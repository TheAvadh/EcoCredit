server.port=8080
# TODO change in prod
spring.datasource.url=${DATASOURCE_URL_PROD}
spring.datasource.username=${DATASOURCE_USER_PROD}
spring.datasource.password=${DATASOURCE_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
logging.level.org.hibernate.type=trace
spring.jpa.properties.hibernate.type=trace

EXPIRATION_DURATION_IN_MS=${EXPIRATION_DURATION_IN_MS}
token.secret.key=${TOKEN_SECRET_KEY}
REFRESH_TOKEN_VALIDITY_MS=${REFRESH_TOKEN_VALIDITY_MS}
mail.username=${EMAIL_USERNAME}
mail.password=${EMAIL_PASSWORD}
mail.host=${EMAIL_HOST}
mail.port=${EMAIL_PORT}
mail.transport.protocol=smtp
mail.smtp.auth=true
mail.smtp.starttls.enable=true
mail.debug=${MAIL_DEBUG_PROD}
password.reset.validity.hours=24

# Prod URL
front-end.server.url=${FRONTEND_SERVER_URL}

signup.verification.token.hours=5000
password.minimum.length=8
